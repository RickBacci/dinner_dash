<h1>Categories Index</h1>

<%= button_to "Create Category", new_admin_category_path, method: :get %>
<% @categories.each do |category| %>
  <%= link_to category.name, admin_category_path(category) %>
  <br>
 <% end %>
