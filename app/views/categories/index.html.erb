<h1>Categories Index</h1>

<% if current_admin? %>
  <%= button_to "Admin Dashboard", admin_dashboard_path, method: :get %>
<% end %>

<% @categories.each do |category| %>
  <%= link_to category.name, category_path(category) %>
  <br>
 <% end %>
